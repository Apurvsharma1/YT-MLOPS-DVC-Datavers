1. Create git repo and clone it to the local.
2. Create mycode.py and add code to it. (it will save a csv file to a new "data" folder)
3  Do a git add-commit-push before initializing dvc. 
# pip install dvc
4. now we do "dvc init" (crates .dvcingnore, .dvc)
5. Now do "mkdir S3" (create a new S3 directory)
6. Now we do " dvc remote add -d myremote S3"
7. Next "dvc add data/" #if we want to do data versioning only on the one file then we do "dvc add data/<filename>
